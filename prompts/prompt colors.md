No componente @src/routes/expedicao/components/separacoes/panel/table-separacoes, implemente um sistema de cores para as linhas da tabela baseado no status da separação. Adicione um botão para que o usuário possa definir as cores que deseja usar para cada status. O sistema de cores deve ser salvo no localStorage. O estilo para as cores deve ser de cores claras para o fundo e cores escuras para o texto. O botão deve ser adicionado como uma opção dentro do botão de engrenagem que já existe no componente. O botão deve ser chamado de "Configurar cores" e deve abrir um modal com as opções de cores para cada status.

