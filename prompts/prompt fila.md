Existe um problema que ao chamar o endpoint /src/routes/api/registrar-volumes/imprimir/+server.ts que chama a api da Sankhya de imprimir etiquetas de volumes onde caso dois usuarios solicitarem a impressão de etiquetas de volumes no mesmo instante, a api retorna a ultima etiqueta de volume para ambos os usuarios. Implemente uma solução para que isso não aconteça. Implemente uma fila de impressão.